apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: prune-old-pipelineruns
spec:
  schedule: "0 11 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: prune-old-pipelineruns
            image: gcr.io/tekton-releases/dogfooding/tkn
            imagePullPolicy: IfNotPresent
            args:
            - /bin/sh
            - -c
            - tkn task start prune-resources --prefix-name prune-pipelineruns -p AGE="10 days" -p RESOURCE_TYPE=pipelinerun
          restartPolicy: OnFailure
          serviceAccountName: pipeline
